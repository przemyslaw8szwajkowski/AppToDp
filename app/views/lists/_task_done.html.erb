<h1>skończone</h1>
<!--dodane :)  -->
 <% @lists_done.each do |list| %>
 <div class="container">
		<div class="row">
			<div class="[ col-xs-12 col-sm-offset-2 col-sm-8 ]">
				<ul class="event-list">
					<li>
						<time datetime="2014-07-20">
							<span class="day"><%= list.created_at.day%></span>
							<span class="month"><%= list.created_at.strftime("%B")%></span>
							<span class="year"><%= list.created_at.year%></span>
						</time>
						<div class="info">
							<h2 class="title"><%= list.name %></h2>
							<p class="desc">Get <%= list.point %> point</p>
							<p class="desc"><%= link_to 'Destroy', list,class: "btn btn-danger btn-lg ", method: :delete, data: { confirm: 'Are you sure?' } %></p>
						</div>
					</li>
				</ul>
			</div>
		</div>
</div>
<% end %>